spring.application.name=Users-ms

#PROFIL
spring.profiles.active=dev

#POSTGRES
spring.datasource.url=jdbc:postgresql://localhost:5432/springms
spring.datasource.username=${POSTGRES_USERNAME}
spring.datasource.password=${POSTGRES_PASSWORD}
spring.datasource.driver-class-name=org.postgresql.Driver
# Hibernate
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

#server port
server.port=${SERVER_PORT:0}

#eureka
eureka.client.service-url.defaultZone=http://localhost:8010/eureka
#popravka dns bug-a
eureka.instance.prefer-ip-address=true

#config server test
config.source=Local

#JWT
token.expiration.time=3600000
token.secret=as878fdss87fc6c987c687sc678sdf876sfdgtrtrhgfd09bv89fds8097dssasd2112336

token.header.prefix=Bearer
token.header.name=Authorization
